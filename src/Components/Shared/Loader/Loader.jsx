const Loader = () => {
  return (
    <div className=" fixed inset-0 flex justify-center items-center h-[100vh] bg-black bg-opacity-50 backdrop-blur-lg overflow-hidden">
      <h1 className=" text-2xl text-primary">Loading . . .</h1>
    </div>
  );
};

export default Loader;
